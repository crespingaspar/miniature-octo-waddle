<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yb.dao.ChathelperDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.yb.entity.ChathelperEntity" id="chathelperMap">
        <result property="ask" column="ask"/>
        <result property="reply" column="reply"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.yb.entity.vo.ChathelperVO" >
		SELECT * FROM chathelper  chathelper         
        <where>${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.yb.entity.vo.ChathelperVO" >
		SELECT  chathelper.* FROM chathelper  chathelper 	
 		<where>${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.yb.entity.view.ChathelperView" >

		SELECT  chathelper.* FROM chathelper  chathelper 	        
        <where>${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.yb.entity.view.ChathelperView" >
		SELECT * FROM chathelper  chathelper <where>${ew.sqlSegment}</where>
	</select>
	


</mapper>
